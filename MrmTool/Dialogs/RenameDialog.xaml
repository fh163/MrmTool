<ContentDialog
    x:Class="MrmTool.Dialogs.RenameDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MrmTool.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{CustomResource Panel.Rename.Title}"
    PrimaryButtonText="{CustomResource Panel.Rename.Button.Rename}"
    CloseButtonText="{CustomResource Common.Button.Cancel}"
    DefaultButton="Primary">

    <Grid VerticalAlignment="Top" Margin="0,5,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{CustomResource Panel.Rename.Label.Name}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,5,5,0" Grid.Column="0"/>
        <TextBox x:Name="nameBox" IsSpellCheckEnabled="False" PlaceholderText="{CustomResource Panel.Rename.Placeholder}" TextChanged="nameBox_TextChanged" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="5,5,0,0" Grid.Column="1"/>
    </Grid>
</ContentDialog>
